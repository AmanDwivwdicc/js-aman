"use strict"; // treat all js code as newer version 

//alert("aman don") not allowed cause were working in nodejs , not browser

let name="aman"
let age=20.2
let isLoggedin=true

console.log(typeof age)