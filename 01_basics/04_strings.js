const name="aman"
const repocount=50
//string define krne ka alag tarika:
const fullName=new String("aman kumar ")
const roll=new Number(56)

console.log(`Hello my name is ${name} and My repo count is ${repocount+10}`);
console.log(name.indexOf("n"));
console.log(roll.toString().length)